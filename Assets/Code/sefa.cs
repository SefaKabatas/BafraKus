using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sefa : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //deneme noktası
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
